function removeExclamationMarks(s) {
  let splitter = s.split("")
  let arr=[]
  for( let i = 0; i < splitter.length; i++){
    if(splitter[i] == "!"){
      let remover = s.replace("!","")
      console.log(remover)
    }else{
      arr.push(splitter[i])
    }
  }
  return arr.join("")
}