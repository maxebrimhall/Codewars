function highAndLow(numbers){
  let result = numbers.split(" ").map((x) => {
    return Number(x)
  })
  let highestResult = Math.max(...result)
  let lowestResult = Math.min(...result)
  return `${highestResult} ${lowestResult}`
}