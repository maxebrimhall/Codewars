class Ship {
  constructor(draft, crew) {
    this.draft = draft
    this.crew = crew
  }
  isWorthIt(){
    let total = this.draft - (this.crew * 1.5)
    if(total > 20){
      return true
      console.log(total)
    }else{
      return false
    }
    }
}